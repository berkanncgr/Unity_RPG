using UnityEngine;
using U_RPG.Resources;

namespace U_RPG.Combat
{
    [RequireComponent(typeof(Health))]
    public class CombatTarget : MonoBehaviour
    {
        // Player can attack if target have this comp.
    }
}